@model List<Entity.Concerete.Car>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}



<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Model</th>
        <th>Category</th>
        <th>Marka</th>
        <th>Vites Türü</th>
        <th>Yıl </th>
        <th>Araba Durumu</th>
        <th>Sil</th>
        <th>Güncelle</th>
        <th>Detaylar</th>
        <th>Sitede Gör</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.CarId</td>
            <td>@item.Model</td>
            <td>@item.CategoryId</td>
            <td>@item.BrandId</td>
            <td>@item.GearType</td>
            <td>@item.Year</td>
            <td>@item.CarStatusId</td>
            <td><a href="/Car/Delete/@item.BrandId" class="btn btn-danger">Sil</a></td>
            <td><a href="/Car/Update/@item.BrandId" class="btn btn-success">Güncelle</a></td>
            <td><a href="#" class="btn btn-secondary">Detaylar</a></td>
            <td><a href="#" class="btn btn-primary">Sitede Gör</a></td>

        </tr>
    }
</table>

<a href="/Car/Add/" class="btn btn-info">Yeni Araba Girişi</a>